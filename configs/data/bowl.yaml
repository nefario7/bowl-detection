_target_: src.data.bowl_datamodule.BowlDataModule
data_dir: data/Bowl.v5i.coco/
batch_size: 8 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: True
